[project]
name = "dpdk-topology-latency-flamegraph"
version = "0.1.0"
description = "Enhance FlameGraph with DPDK coloring, funclatency, topology, and kernel interaction signals"
requires-python = ">=3.9"

[project.scripts]
dpdk-fg = "dpdk_fg.cli:main"
